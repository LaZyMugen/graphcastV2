dataset:

  input_steps: 4
  forecast_steps: 1

normalization:

  epsilon: 1.0e-8

graph:

  connectivity: 4

training:

  epochs: 5
  batch_size: 1
  learning_rate: 0.001
  weight_decay: 0.0001

  num_workers: 0
  shuffle: true

model:

  hidden_dim: 64
  graph_layers: 4